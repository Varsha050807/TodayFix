import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TodayFix.in - Verified Local Business Directory & Service Experts",
  description: "Find verified plumbers, electricians, web developers, doctors and local businesses in your city with ratings, contacts, and zero broker fees. Connect directly.",
  keywords: "local directory, home services, verified plumber, verified electrician, web developer, doctors near me, appliance repair, TodayFix",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
